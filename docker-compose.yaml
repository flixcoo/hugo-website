name: "hugo-website"

services:
  hugo:
    build: .
    image: "hugo-website:latest"
    container_name: "hugo-website"
    restart: unless-stopped
    ports:
      - "1313:1313"
    networks:
      - web

networks:
  web:
    external: true